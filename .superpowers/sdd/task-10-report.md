# Task 10 — Footer Rewrite

## Completed

- Rewrote `components/Footer.tsx` with dark background (`bg-dark-charcoal`), brand name, social links, copyright, and a scroll-to-top button.
- Added `"use client"` with `useState`/`useEffect` scroll detection (`showTop` at >400px) and `AnimatePresence` + `motion.button` for the back-to-top FAB.
- Build (`npm run build`) — passed
- Lint (`npm run lint`) — passed

## Commit

```
5d8177a feat: rewrite footer with dark bg and back-to-top button
```
