# Task 11 — Gallery Integration and Scroll-Reveal Wrapping

## Completed

- Added `Gallery` component import and `<ScrollReveal><Gallery /></ScrollReveal>` to page layout.
- Wrapped all section components (`Hero`, `About`, `Menu`, `Gallery`, `Ambiance`, `Location`) in `<ScrollReveal>`.
- Deleted default Next.js SVGs (`file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg`).
- Build (`npm run build`) — passed
- Lint (`npm run lint`) — passed

## Commit

```
a082c31 feat: add gallery to page, wrap sections with scroll-reveal, clean up default assets
```
